# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Build artifacts
build/
dist/

# Terraform
.terraform/
# Only the deployable root pins providers; the module and examples do not.
.terraform.lock.hcl
!deploy/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
# Keep the provider lock file committed for reproducible deploys.

# SDD scratch
.superpowers/

# OS
.DS_Store

# uv (not used; deps installed via pip in CI)
uv.lock
